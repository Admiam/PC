#include "tree.h"
#include <stdlib.h>

int main(){
    node *root = NULL;
    int i, keys[] = { 5, 3, 2, 3, 4, 5, 9};

    for (i = 0; i < KEYS_LEN; i++) {
        node node_add(&rood, keys[i]);
    }



    return 0;
}